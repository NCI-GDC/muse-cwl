#!/usr/bin/env cwl-runner

description: |
  Usage:  cwl-runner <this-file-path> XXXX
  Options:
    --tumor_bam_path       tumor bam path
    --normal_bam_path      normal bam path
    --uuid                 uuid
requirements:
  - import: node-engine.cwl
  - import: envvar-global.cwl
  - class: DockerRequirement
    dockerPull: quay.io/ncigdc/muse-tool

class: CommandLineTool

inputs:
  - id: "#tumor_bam_path"
    type: File
    inputBinding:
      prefix: --tumor_bam_path
      secondaryFiles:
        - engine: node-engine.cwl
          script: |
            {
            return {"path": $self.path.slice(0,-4)+".bai", "class": "File"};
            }

  - id: "#normal_bam_path"
    type: File
    inputBinding:
      prefix: --normal_bam_path
      secondaryFiles:
        - engine: node-engine.cwl
          script: |
            {
            return {"path": $self.path.slice(0,-4)+".bai", "class": "File"};
            }

  - id: "#reference_fasta_name"
    type: File
    inputBinding:
      prefix: --reference_fasta_name
      secondaryFiles:
        - engine: node-engine.cwl
          script: |
            {
            return {"path": $self.path.slice(0,-3)+".dict", "class": "File"};
            }

  - id: "#reference_fasta_fai"
    type: File
    inputBinding:
      prefix: --reference_fasta_fai

  - id: "#thread_count"
    type: string
    inputBinding:
      prefix: --thread_count

  - id: "#Parallel_Block_Size"
    type: string
    inputBinding:
      prefix: --Parallel_Block_Size

  - id: "#uuid"
    type: string
    inputBinding:
      prefix: --uuid


outputs:
  - id: "#output_list"
    type:
      type: array
      items: File
    outputBinding:
      glob: output.*.MuSE.txt

  - id: "#output_sqlite"
    type: File
    description: "sqlite file"
    outputBinding:
      glob:
        engine: node-engine.cwl
        script : |
          {
          return $job['uuid']+".db"
          }

  - id: "#log"
    type: File
    description: "python log file"
    outputBinding:
      glob:
        engine: node-engine.cwl
        script: |
          {
          return $job['uuid']+"_call.log"
          }

baseCommand: ["/home/ubuntu/.virtualenvs/p3/bin/python","/home/ubuntu/tools/muse-tool/main.py", "--tool_name", "muse_call"]
